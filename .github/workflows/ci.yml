name: CI Pipeline

on:
  push:
    branches:
      - main
      - 'feature/**'
      - 'fix/**'
    paths-ignore:
      - '**.md'
      - '.gitignore'
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  lint:
    name: Lint & Validate
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate config exists
        run: |
          if [ -f "config.txt" ]; then
            echo "✓ config.txt found"
          else
            echo "✗ config.txt missing"
            exit 1
          fi

      - name: Validate workflow file exists
        run: |
          if [ -f ".github/workflows/ci.yml" ]; then
            echo "✓ ci.yml found"
          else
            echo "✗ ci.yml missing"
            exit 1
          fi

  test:
    name: Test
    runs-on: ubuntu-22.04
    needs: lint

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run simulated tests
        run: |
          echo "Running unit tests..."
          sleep 1
          echo "✓ Unit tests passed"

      - name: Set test result output
        id: test-result
        run: echo "result=passed" >> $GITHUB_OUTPUT

      - name: Print test result
        run: |
          echo "Test result: ${{ steps.test-result.outputs.result }}"

  compatibility-check:
    name: Check on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, ubuntu-20.04]
        config-file: ['config.txt']

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check config on ${{ matrix.os }}
        run: |
          echo "Running on: ${{ matrix.os }}"
          echo "Checking: ${{ matrix.config-file }}"
          if [ -f "${{ matrix.config-file }}" ]; then
            echo "✓ ${{ matrix.config-file }} exists on ${{ matrix.os }}"
          else
            echo "✗ ${{ matrix.config-file }} missing on ${{ matrix.os }}"
            exit 1
          fi

  summary:
    name: Pipeline Summary
    runs-on: ubuntu-22.04
    needs: [lint, test, compatibility-check]

    steps:
      - name: Print summary
        run: |
          echo "==============================="
          echo "Pipeline completed successfully"
          echo "Commit: ${{ github.sha }}"
          echo "Branch: ${{ github.ref }}"
          echo "Actor:  ${{ github.actor }}"
          echo "==============================="

      - name: Notify on failure
        if: failure()
        run: |
          echo "Pipeline failed — would notify Slack here in a real pipeline"